<div class="container">
  <div class="row">
    <div class="col">
      <h1 class="text-center">Users</h1>

      <% @users.each do |user| %>
        <%= link_to user.username, profile_path(user) %><br>
      <% end %>
    </div>
  </div>
</div>
